# Kemet AI Models as Microservices

This project holds a family of AI microservices.

## Deployment

* Connect to the deployment server and make your own `.env` file with the required keys.

* Run Docker compose from the [docker-compose.yml](../docker-compose.yml) repository.
